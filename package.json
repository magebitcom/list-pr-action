{
  "name": "list-pr-action",
  "version": "1.0.0",
  "description": "GitHub Action to list pull requests for a specific branch or tag",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "format": "prettier --write **/*.ts"
  },
  "keywords": ["github", "actions", "pull-requests"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3",
    "prettier": "^3.1.1"
  }
}
